spring: 
  application:
    name: student-provider-config
  cloud:
    config:
      name: provider_config
      uri: http://localhost:6001  # 配置configserver地址
      profile: dev  # 级别
      label: master  # 分支 git中 默认master