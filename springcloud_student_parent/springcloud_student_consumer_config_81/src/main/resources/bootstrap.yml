spring: 
  application: 
    name: consumer-dev
  cloud: 
    config: 
    # name和profile
      name: consumer_config
      uri: http://localhost:6001/  # 配置configserver地址
      profile: test  # 级别
      label: master  # 分支 git中 默认master
      fail-fast: true